# coturn TURN/STUN 服务器配置
# 部署: apt install coturn
# 运行: turnserver -c turnserver.conf

listening-port=3478
# 公网 IP（TURN 中继需要）
external-ip=<YOUR_PUBLIC_IP>

fingerprint
lt-cred-mech
realm=scrcpy-cloud

# 用户名:密码（客户端 ICE 配置使用）
user=turnuser:turnpassword

# 日志
verbose
